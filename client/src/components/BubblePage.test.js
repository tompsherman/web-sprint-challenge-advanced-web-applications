import React from "react";
import { render, screen } from "@testing-library/react";
import BubblePage from "./BubblePage";

describe("BubblePage Tests", ()=>{
  test("render without error", ()=>{
    render (<BubblePage/>)
  })
  test("Fetches data and renders the bubbles", () => {
  
})
})